## 加载logo
cat /root/logo_banner.txt | lolcat

### 自定义函数命令
function help {
    if [ -z "$1" ]; then
        cat ~/.cxyhelp | lolcat
    else
        cat ~/.cxyhelp | grep $1 | lolcat
    fi
}

alias q='exit'